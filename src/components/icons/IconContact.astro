---
import IconLinkContainer from './IconLinkContainer.astro';
export interface Props {
	href: string;
	title: string;
}
const { href, title } = Astro.props;
---

<IconLinkContainer href={href} title={title} height="h-12" width="w-12">
	<slot />
</IconLinkContainer>
