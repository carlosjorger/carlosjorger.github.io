---
export interface Props {
	left?: boolean;
	center?: boolean;
	right?: boolean;
	class?: string;
}
const { center = false, left = false, right = false } = Astro.props;
---

<h2
	class="my-2 text-4.5xl font-bold tracking-wide text-[var(--default-font-color)]"
	class:list={[
		{ 'text-left': left, 'text-center': center, 'text-right': right },
		Astro.props.class,
	]}
>
	<slot />
</h2>
