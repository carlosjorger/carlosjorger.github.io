---
import WidthContainer from './WidthContainer.astro';
export interface Props {
	id?: string;
	class?: string;
}
const { id } = Astro.props;
---

<WidthContainer>
	<section id={id} class="z-[2] pt-7" class:list={[Astro.props.class]}>
		<slot />
	</section>
</WidthContainer>
